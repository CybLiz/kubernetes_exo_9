apiVersion: v1
kind: Service

metadata:
  name: postgres-service
  namespace: postgres-ns

spec:
  type: NodePort

  selector:
    app: postgres
    tier: database

  ports:
    - port: 5432
      targetPort: 5432
      nodePort: 30007